@page "/splash"
@inject NavigationManager Nav

<h1>Mathilda</h1>
<p>Loading your travel companion...</p>

@code {
    protected override void OnInitialized()
    {
        // The startup intro, privacy consent, and location prompt are handled by MainLayout.
        // Hand straight off to the dashboard.
        Nav.NavigateTo("/");
    }
}
